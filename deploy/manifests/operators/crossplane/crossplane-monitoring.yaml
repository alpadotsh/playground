apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: pmon
  namespace: crossplane-system
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: crossplane
  podMetricsEndpoints:
    - port: metrics
      interval: 30s